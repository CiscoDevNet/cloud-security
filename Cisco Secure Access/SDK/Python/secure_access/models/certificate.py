# coding: utf-8

"""
    Cisco Secure Access API

    Unified OpenAPI spec for Cisco Secure Access APIs.

    The version of the OpenAPI document: 1.0.0
    Contact: devnet@cisco.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Certificate(BaseModel):
    """
    The properties of the device certificate.
    """ # noqa: E501
    certificate_id: StrictStr = Field(description="The unique identifier of the ACME-issued certificate.", alias="certificateId")
    created_at: datetime = Field(description="The date and time (in ISO 8601 format) when the ACME-issued certificate was created.", alias="createdAt")
    expires_at: datetime = Field(description="The date and time (in ISO 8601 format) when the ACME-issued certificate expires.", alias="expiresAt")
    revoked_at: Optional[datetime] = Field(default=None, description="The date and time (in ISO 8601 format) when the ACME-issued certificate was revoked.", alias="revokedAt")
    status: StrictStr = Field(description="The status of the ACME-issued certificate.")
    __properties: ClassVar[List[str]] = ["certificateId", "createdAt", "expiresAt", "revokedAt", "status"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'expired', 'revoked']):
            raise ValueError("must be one of enum values ('active', 'expired', 'revoked')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Certificate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Certificate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "certificateId": obj.get("certificateId"),
            "createdAt": obj.get("createdAt"),
            "expiresAt": obj.get("expiresAt"),
            "revokedAt": obj.get("revokedAt"),
            "status": obj.get("status")
        })
        return _obj


